<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.coopappiltda.coopappiltda">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/coopappi_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/coopappi_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup"
        tools:targetApi="n">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".ListaFacturacion" />
        <activity android:name=".Facturacion" />
        <activity android:name=".Notificaciones" />
        <activity android:name=".Editar" />
        <activity android:name=".Delete" />
        <activity android:name=".historial" />
        <activity android:name=".Requisito4" />
        <activity android:name=".Requisito3" />
        <activity android:name=".Requisito2" />
        <activity android:name=".Aviso_de_cobro" />
        <activity android:name=".CortexDeuda" />
        <activity android:name=".ListSociosAvisoCobranza" />
        <activity android:name=".ListSociosCortePorDeuda" />
        <activity android:name=".Requisito1" />
        <activity android:name=".Requisitos" />
        <activity android:name=".Ver_tuconsumo" />
        <activity android:name=".Tu_consumo" />
        <activity android:name=".ConsultaFacturas" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>